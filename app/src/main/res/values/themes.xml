<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WorkingShifts" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/backgr_im</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item> <!-- Для прозрачности статус-бара -->
        <item name="android:windowTranslucentNavigation">true</item> <!-- Для прозрачности навигационной панели -->

        <!-- Цвет линии, когда EditText активен -->
        <item name="colorControlActivated">@color/dark_blue</item>
        <!-- Цвет подчеркивания -->
        <item name="colorAccent">@color/white</item>
<!--        &lt;!&ndash; Цвет линии, когда EditText не активен &ndash;&gt;-->
<!--        <item name="colorControlNormal">@color/your_custom_color</item>-->
    </style>

    <style name="Theme.WorkingShifts" parent="Base.Theme.WorkingShifts" />



</resources>